# 项目 STAR 陈述（简历 + 面试背诵版）

> 严格基于代码事实与评测数据，无虚构夸大。所有数字均可在 `eval_report.json` 和 `runs/agent_trace_*.jsonl` 中复现。

---

## 一句话简介（简历"项目概述"栏用）

独立设计并实现一个面向 WiFi CSI 感知领域的 **RAG + 多工具 ReAct Agent** 系统，支持单跳问答与多步调研两种模式，检索准确率较单路向量检索提升 **35.9%（MRR 0.584→0.793）**，通过工具治理层（配额/去重/熔断器）将不可用网络场景下的调研耗时从 **269s 降至 75s**。

---

## S - 情境（Situation）

WiFi CSI 人体感知领域近年来论文数量激增，但知识点分散在数十篇 PDF/Word 中，新人入门或研究人员做对比调研时需要逐篇翻阅，效率极低。传统搜索引擎无法理解领域术语（如"CSI 振幅相位解耦""跨站点泛化"），而直接用 LLM 回答会产生幻觉且无法给出处。

**核心挑战**：如何让系统既理解领域术语，又能对每个结论给出文献出处，同时支持需要多角度检索的"调研报告"类复杂问题。

---

## T - 任务（Task）

1. 构建一个能基于本地学术文献库回答问题的 RAG 系统，答案必须附带文献来源和页码
2. 对需要"全面调研"的复杂问题，系统能自动拆解子问题、调用多工具检索、汇总证据输出结构化报告
3. 保证 Agent 不陷入死循环、不重复调用相同工具、不在不可用工具上浪费时间

---

## A - 行动（Action）

### 1. 检索层：从单路到混合 RRF

**问题**：纯向量检索在领域术语上表现差（MRR 仅 0.584），BM25 虽然 Hit@1 高（0.733）但 Hit@5 只有 0.767。

**方案**：实现 FAISS 向量检索 + BM25 关键词检索的 **RRF（Reciprocal Rank Fusion）混合**，对比了 0.3/0.7 ~ 0.7/0.3 五组权重。

**发现**：0.5/0.5 权重最优（MRR 0.793），且 **Cross-Encoder 重排（bge-reranker-large）反而降低准确率**（MRR 0.751）——因为 reranker 倾向于把"综述型段落"排到前面，挤压直接证据块。默认关闭重排，保留为可切换选项。

### 2. Agent 决策：从正则解析到原生 Function Calling

**问题**：早期版本用 `re.search(r"\{.*\}", text, re.S)` 解析 LLM 输出的工具调用 JSON，遇到模型输出不规范格式时频繁解析失败。

**方案**：改用 DeepSeek 原生 `tool_calls` 协议，用 `bind_tools + pydantic schema` 让 LLM 直接返回结构化参数，解析和校验由框架负责。

### 3. 工具治理：让 Agent "可控"

| 治理机制 | 解决的问题 | 实现 |
|---|---|---|
| **独立配额** | Agent 反复用同一工具 | retrieve 4 次 / arxiv 3 次 / python 2 次，用完自动停用 |
| **重复调用拦截** | 同义改写 query 浪费检索 | 用 SequenceMatcher 算相似度，≥0.85 直接拦截并回灌提示 |
| **熔断器** | 工具不可用时反复重试浪费时间 | 同一工具连续失败 2 次，本轮后续调用全部拦截 |
| **预算强制收尾** | 证据足够却不结束 | 证据字符超 8000 或所有工具配额耗尽，用 `tool_choice="finish"` 强制收尾 |

**典型案例**：arXiv 在国内网络不可达，每次 `URLError` 耗 40s。没有熔断器时，模型连续尝试 5 次浪费 200s，总耗时 269s；加熔断器后连续失败 2 次即停用，总耗时降至 75s。

### 4. 可观测：每步 trace JSONL

每次 Agent 运行把思考、工具调用、观察、耗时、配额消耗逐行写入 `runs/agent_trace_*.jsonl`，Web 端实时渲染思考链。这让"Agent 为什么这么决策"可审计，而不是黑盒。

---

## R - 结果（Result）

### 检索质量（36 题标注测试集）

| 指标 | 向量检索 | BM25 | **混合 RRF 0.5/0.5** | 混合+reranker |
|---|---|---|---|---|
| MRR@5 | 0.584 | 0.744 | **0.793** | 0.751 |
| Hit@1 | 0.467 | 0.733 | **0.733** | 0.667 |
| Hit@3 | 0.633 | 0.767 | **0.867** | 0.833 |
| 平均延迟 | 54ms | 9ms | 39ms | 13.6s |

### 生成质量

- 关键词覆盖率：**0.678**（回答覆盖证据关键词的比例）
- 拒绝准确率：**1.0**（对 6 道不可回答问题全部正确拒答，无幻觉）
- 平均生成延迟：1.05s

### Agent 治理效果

| 场景 | 无治理 | 有治理 |
|---|---|---|
| arXiv 不可达时的调研耗时 | 269s（白试 5 次） | **75s**（2 次失败后熔断） |
| 重复 query 检索 | 重复执行 | 相似度 ≥0.85 拦截 |
| 证据足够却不结束 | 可能跑满 MAX_STEPS | 预算超限强制 finish |

---

## 面试高频追问准备

### Q1：为什么用 RAG 而不是微调？

**答**：三个原因——① 微调需要大量领域标注数据，我们只有 32 篇论文，不够支撑微调；② RAG 的答案可以溯源到具体文献页码，这对学术场景是硬需求，微调做不到；③ 成本：调用 DeepSeek API 一次约 0.01 元，而微调一个 7B 模型需要几千元，且模型迭代慢。RAG 是"用检索换微调"的性价比之选。

### Q2：为什么混合检索用 RRF 而不是加权分数融合？

**答**：向量检索和 BM25 的分数尺度完全不同（向量是余弦相似度 0~1，BM25 是无界整数），加权融合需要归一化且权重难调。RRF 只看排名（`1/(k+rank)`），天然尺度无关，0.5/0.5 权重就能达到最优。实测 RRF 比加权融合的 MRR 高 2-3 个点。

### Q3：熔断器和配额的区别？

**答**：配额是"成功调用次数上限"——工具正常工作但不能无限用。熔断器是"连续失败后停用"——工具本身不可用了，再试也是浪费。两者互补：配额防 Agent 滥用，熔断器防 Agent 撞墙。

### Q4：怎么验证 Agent 比单跳 RAG 好？

**答**：目前用的是 trace 审计 + 人工对比。单跳 RAG 对"对比 X 和 Y"这类问题只能检索一次，容易遗漏；Research 模式会拆成多个子问题多角度检索，证据数通常是单跳的 3-4 倍。下一步计划加 faithfulness 评测（答案是否真来自证据）和 tool_call_accuracy 来量化。

### Q5：如果让你重新设计，会做什么不同？

**答**：① 会从一开始就用 LangGraph 而不是手写 harness——手写让我理解了 ReAct 每个环节，但生产环境 LangGraph 的状态管理和中断恢复更成熟；② 会更早引入评测集——前期调参全靠肉眼看，有了 36 题标注集后才发现 reranker 反而降准；③ 会把工具治理做成独立中间件，而不是嵌在主循环里，方便复用。
